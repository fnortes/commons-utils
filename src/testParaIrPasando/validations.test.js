// import {
//   date
// } from "../src/validations";

describe("it should exist a date.isValid function", () => {
  it("should exist the method", () => {
    //     expect(date.isValid).not.toBeNull();
  });

  // it("should return false", () => {
  //   expect(date.isValid()).toBe(false);
  //   expect(date.isValid(null)).toBe(false);
  //   expect(date.isValid("")).toBe(false);
  //   expect(
  //     date.isValid({
  //       test: 1,
  //     })
  //   ).toBe(false);
  //   expect(date.isValid(2)).toBe(false);
  //   expect(date.isValid(-2)).toBe(false);
  // });

  // it("should return true", () => {
  //   expect(date.isValid("Wed Dec 23 2020 18:07:45")).toBe(true);
  //   // expect(date.isValid("Sat Dec 23 2020 18:07:45")).toBe(false);
  //   // expect(date.isValid("Wed Eee 23 2020 18:07:45")).toBe(false);
  //   expect(date.isValid("15/07/2018", "DD/MM/YYYY")).toBe(true);
  // });
});
